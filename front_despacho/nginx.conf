server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Esto es vital para aplicaciones React (SPA)
        try_files $uri $uri/ /index.html;
    }
}